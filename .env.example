# OpenSea API Configuration
OPENSEA_API_KEY=your_opensea_api_key_here
ALCHEMY_API_KEY=your_alchemy_api_key_here
RESERVOIR_API_KEY=your_reservoir_api_key_here

# ===================================================================
# ENCRYPTION CONFIGURATION - SECURITY CRITICAL
# ===================================================================
#
# These variables control the encryption of your private keys.
# The security of your encrypted key storage depends on these values.
#
# ⚠️  IMPORTANT SECURITY NOTES:
# • Use strong, unique values for each parameter
# • Store these values securely (password manager, encrypted vault)
# • Never commit these values to version control
# • Change these values only if you understand the migration process
# • Longer passwords and salts provide better security

# Primary encryption password - MINIMUM 32 characters recommended
# Should be a high-entropy string unique to your installation
ENCRYPTION_PASSWORD=change_this_to_a_strong_password_minimum_32_chars

# Unique salt value for key derivation - MINIMUM 16 characters
# Should be different from the encryption password
ENCRYPTION_SALT=change_this_to_a_unique_salt_minimum_16_chars

# scrypt key derivation parameters (memory-hard algorithm)
# Higher values = better security but slower performance
#
# ENCRYPTION_ITERATIONS: CPU/memory cost factor (default: 32768)
# - Minimum: 16384
# - Recommended: 32768+ for production
# - Higher values significantly slow down brute force attacks
ENCRYPTION_ITERATIONS=32768

# ENCRYPTION_MEMORY: Memory limit in bytes (default: 134217728 = 128MB)
# - Minimum: 67108864 (64MB)
# - Recommended: 134217728 (128MB) or higher
# - Ensure your system has sufficient RAM
ENCRYPTION_MEMORY=134217728

# ENCRYPTION_PARALLELISM: Parallel threads (default: 1)
# - Typically 1 for single-threaded operations
# - Increase only if you have multiple CPU cores
ENCRYPTION_PARALLELISM=1

# ===================================================================
# OPTIONAL: PROXY CONFIGURATION (for international users)
# ===================================================================
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890